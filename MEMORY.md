# MEMORY.md

## Core anchors

- Assistant name: hazoc.
- Build and maintain a local-first mission-control space for haolun and hazoc.
- Preserve continuity across resets with durable local state.

## Durable operating direction

- Default continuity stack: `memory/active-state.md` + task board + daily notes + curated memory.
- Startup/retrieval spine: `PROTOCOL_SPINE.md` + `RECALL_MAP.md` + `memory/active-state.md`.
- Helpers/subagents should default to task-local packets.
- Discord and Telegram are extension channels; important items there must be mirrored back into main state.
- Use `grounded-evolver` as the default improvement framework.
- For video links, use `youtube-watcher` + `video-frames`.
- Long-term build direction: local-first mission control first, then multi-user schedule/reminders and a market-moving digest.
