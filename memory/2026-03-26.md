- Automated GitHub sync succeeded; pushed commit `01bea89`.
- Hourly memory maintenance: confirmed passive work in the last hour was the GitHub sync commit `01bea89`; archived the stale past-due "Speech Revision" event so events state matches current reality.
- Re-anchored current context after a fresh session reset from active-state, today's memory, curated memory, and the mission-control task board. Live workbench priorities remain mission-control UI polish, market-moving news digest design, and multi-user schedule architecture; the main open issue is still that the UI works but feels visually clunky.
- Discord request from haolun in #control-center: set a one-shot reminder for today at 8:30 PM America/New_York to say exactly "Get English Speech Booking Done". Scheduled the cron reminder in the current Discord session and mirrored the request into mission-control events.
- Follow-up lesson: that reminder was wired through an internal main-session `systemEvent` path, so it triggered as internal reminder handling instead of producing a guaranteed visible Discord ping. Promoted the fix into `AGENTS.md` and `TOOLS.md`: for future in-channel reminders, use explicit chat-delivery / announce behavior instead of relying on an internal system event alone.
- New mission-control polish direction from haolun: remove self-explanatory page/context boxes that just restate what the pages are for. The UI should stay utility-first and assume shared understanding instead of explaining the product back to us.
- Further mission-control UI preference from haolun: make selected task/detail state much clearer and smoother. Implemented a stronger selected-card treatment with a brighter surface, bold accent edge, and larger selection ring so opening task/event/memory details feels more obvious.
- Quick follow-up preference from haolun: the explicit Selected badge was overkill. Removed it and kept the clearer selection through the card treatment itself.
- Further task-list polish: archive selection should not feel choppy, and the task-section menu highlight should follow each lane's own color scheme. Updated the active styles to be lane-aware so Workbench stays warm, On Hold stays plum, and Archived stays green.
- Sidebar / whole-site polish pass: made the active nav item feel more clearly selected with a richer gold-dot + accent treatment, and added a few low-risk quality upgrades across the app (smoother hover states on clickable cards and cleaner custom scrollbars on dense panels/lists).
- Archive task selection still felt undercooked after that pass. Tightened it again so archived rows use a stronger green-tinted active state across the full card treatment, including the selected fill, left accent edge, and active metadata chips.
- Automated GitHub sync succeeded; pushed commit `1240e7e`.
- Haolun explicitly gave hazoc broad discretion to improve mission control visually/usability-wise, with the expectation of cohesive polish rather than narrow one-off tweaks. The current preference is for thoughtful, utility-first refinement that stays within the established dark luxe palette.
- Current likely next high-value polish targets: lighten/simplify the top bar, improve spacing/hierarchy in the detail pane, and make the schedule/calendar page easier to scan at a glance.
- Important correction from haolun on the Tasks > Archived view: the real issue was structural compression, not just the highlight color. Archived task rows were too squished and the section dropdown became unusably tiny. Reworked Archived into a more compact shelf layout with full-width rows, less duplicated metadata, and a real-width dropdown instead of reusing the active-task card layout unchanged.
- Further task-control polish request from haolun: the native browser dropdown menus themselves looked ugly. Replaced the task/event section/status selects with custom in-app dropdown menus so the open state matches the mission-control palette instead of falling back to default browser UI.
- Follow-up fix: forcing the task-row menu into a drop-up caused the top card menu to be clipped by the surrounding container. Switched the task-row section control to smart auto-placement so it opens downward when there is room and only flips upward when needed.
- Another dropdown polish fix: the custom menu still felt too translucent over the task cards, causing visual bleed-through. Increased menu opacity/separation so it reads like a solid floating panel instead of a glassy overlay.
- Final control redesign: even the improved floating menu still conflicted with card text, so the better solution was to stop using popup menus here. Replaced task lane and task/event status menus with inline segmented controls that stay inside the card/form layout and match the lane color scheme without obscuring content.
- Follow-up segmented-control refinement: the dot made the switch feel visually awkward and slightly misaligned. Simplified the controls into cleaner text-first pills with calmer spacing and active states, removing the dot so the switch reads more polished.
- Important UX correction from haolun: the task lane change for "Optimize the mission-control website" really did persist to Archived after an accidental click. Moved it back to Workbench. Also fixed the detail-pane section/status controls so they apply immediately instead of pretending to be interactive while actually waiting for the separate Save button; title/notes/schedule/memory fields still use explicit Save.
- Automated GitHub sync succeeded; pushed commit `b96c0f2`.
- Grounded-evolver was upgraded from a signal→score→mutate flow to a signal→generalize→score→mutate flow. New required step: identify the root cause, generalize it into a reusable moral/guideline, and only then choose the smallest mutation. The planner script now emits both a root-cause summary and a generalized moral before recommending actions.
- New mission-control feature direction from haolun: add a read-only Skills tab that shows installed skills and a flow-chart-style view of how hazoc reasons through each one, so the process can be inspected and improved. Implemented the first version as a live Skills page backed by a new `/api/skills` endpoint that scans installed skill folders, lists skills, and renders extracted thinking-flow steps per skill.
- First Skills-page follow-up from haolun: the left-side skill bubbles/cards were too cramped to read. Reworked the list into two sections (user-edited/created vs system) and gave the cards more breathing room with clearer headers and less cramped text density.
- Further Skills-page direction from haolun: use a more intentional template-style flowchart for process-heavy skills, but do not force chart views onto every skill. Shifted the page toward curated algorithm charts for grounded-evolver and learning-loop specifically, while non-algorithmic/reference-heavy skills now stay in a lighter reference-style process view.
- Next Skills-page refinement from haolun: explicitly determine which workspace/user-created skills are graph-worthy and give the chart area enough room to fit authored graphs without internal scrolling. Current graph-worthy set marked in the UI: grounded-evolver, learning-loop, safe-evolution-loop, workspace-continuity, workspace-memory-stack, and workspace-graph.
- Follow-up Skills-page work: authored the next chart for workspace-memory-stack and changed the chart renderer to scale up to available panel width so the graph uses the large empty detail-pane space instead of staying stuck at a small fixed size.
- Further chart-presentation refinement from haolun: the graph box should stay large, but the boxes/nodes inside the chart should feel comfortably smaller. Adjusted the chart renderer to center the chart within the pane with deliberate gutters and slightly reduced node sizing so the diagram uses the space without feeling oversized.
- New chart-design rules from haolun for current/future skill graphs: keep all text fully inside containers, avoid line intersections/merging when branches are meant to stay distinct, keep branch lines aligned rather than awkwardly staggered, and judge charts first for clarity/legibility and then for elegance. Applied the first pass of these rules by adding edge-anchor routing, shorter/wrapped decision labels, and cleaner branch geometry in the authored charts.
- Additional chart polish rule from haolun: streamline arrows so they take the most direct readable route with no unnecessary turns. Updated the authored charts to use simpler side-anchored routes and fewer elbows, especially around decision branches.
- Further chart-routing correction from haolun: arrows must be strictly horizontal/vertical with rounded turns, never diagonal. Replaced the raw polyline connector rendering with rounded orthogonal path generation so future chart edges default to right-angle routing with curved corners.
- New durable chart rule from haolun: any arrow from one point to another should take at most one total turn by default. Promoted this into the workspace operating rules so future skill charts favor one-turn orthogonal routing instead of multi-elbow connectors unless there is a strong reason otherwise.
- Current chart cleanup pass was updated to match that rule structurally: authored skill-chart edges now use single-elbow orthogonal routing inputs, rounded orthogonal path rendering, and smaller node footprints so charts stay inside the box without clipping.
- Additional durable lesson from this chart-work failure: internal renderer/schema cleanup does not count as a real UI fix unless the rendered result visibly solves the complaint. Promoted a new AGENTS rule to require checking the actual rendered outcome after each meaningful UI/layout pass before declaring success.
- End-of-day handoff: the immediate next-day priority is still the Skills-page chart layout pass. Renderer/routing constraints were tightened (orthogonal, rounded, one-turn schema, smaller nodes, bounds-aware canvas), but the rendered geometry still needs a proper visual re-layout around those rules before the chart work can be considered done.
- Local wrap-up state: uncommitted workspace changes remain in `AGENTS.md`, `memory/2026-03-26.md`, `mission-control/data/tasks.json`, `mission-control/src/App.jsx`, and `mission-control/src/styles.css` (plus maintenance state), so tomorrow should resume from the current working tree rather than assuming a clean checkout.
- Automated GitHub sync succeeded; pushed commit `8406ee1`.
- Automated GitHub sync succeeded; pushed commit `a516f33`.
